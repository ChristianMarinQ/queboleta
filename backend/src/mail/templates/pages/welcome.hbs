<html lang='es'>
  <head>
    <meta charset='utf-8' />
    <title>Bienvenido</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        background: #ffffff;
        font-family: monospace, monospace;
        color: #000000;
      }
      .email {
        max-width: 600px;
        margin: 40px auto;
        border: 1px solid #000;
      }
      .inner {
        padding: 24px;
      }
      h1 {
        font-size: 20px;
        margin: 0 0 16px 0;
        font-weight: 700;
        text-transform: uppercase;
      }
      p {
        margin: 0 0 12px 0;
        line-height: 1.4;
      }
      .btn {
        display: inline-block;
        margin-top: 12px;
        padding: 12px 16px;
        border: 1px solid #000;
        text-decoration: none;
        color: #000;
        font-weight: 700;
        text-transform: uppercase;
      }
      .coupon {
        display: block;
        margin: 16px auto;
        padding: 10px;
        text-align: center;
        font-weight: 700;
        border: 1px dashed #000;
        text-transform: uppercase;
      }
      .footer {
        border-top: 1px solid #000;
        padding: 12px 24px;
        font-size: 12px;
      }
    </style>
  </head>
  <body>
    <div class='email'>
      <div class='inner'>
        <h1>Bienvenido a QueBoleta</h1>
        <p>Hola
          {{fullnames}}, gracias por registrarte en nuestra plataforma.</p>
        <p>Activa tu cuenta para comenzar a usar todas las funciones:</p>
        <a href='{{activationLink}}' class='btn'>Activar cuenta</a>
        <p style='margin-top:20px;'>Además, aquí tienes un cupón de descuento:</p>
        <span class='coupon'>{{couponCode}}</span>
        <p>Úsalo en tu primera compra.</p>
      </div>
      <div class='footer'>QueBoleta — Bienvenida</div>
    </div>
  </body>
</html>
